# Apply to DSE ERP 2.1.3

Create a feature branch from the canonical GitHub `main`, copy the included paths over
the repository, commit, merge into `develop`, run the daily Windows/macOS verification,
and only then merge to `main` and create the GitHub Release.

This package is cumulative: it contains every file changed by the final stabilization
relative to the uploaded 2.1.3 canonical IntelliJ baseline.
