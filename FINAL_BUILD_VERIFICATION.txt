DSE ERP 9.0.85 — FINAL BUILD / DEPLOYMENT + DISASTER-RECOVERY VERIFICATION
============================================================================

RELEASE
- Baseline: DSE ERP 9.0.84 automated deployment source.
- Release: 9.0.85.
- Scope: Oracle deployment protected-file fix + explicit Server -> LOCAL disaster recovery.
- No business schema, sidebar/navigation, or normal ERP workflow redesign.

DEPLOYMENT FIX
- scripts/linux/deploy-oracle-release.sh reads /etc/dse-erp/<env>.env through sudo -n.
- /etc/dse-erp/<env>-db-password remains protected and is read through sudo -n only on Oracle.
- GitHub never receives the PostgreSQL password.
- Existing SHA-256, pre-upgrade backup, versioned-release, health and automatic binary-rollback gates remain.

DISASTER RECOVERY
- Fresh administrator-only server recovery package endpoint.
- Recovery package = validated PostgreSQL snapshot + Attachments + Documents + Templates.
- Credentials/environment files are excluded.
- Shared Client never automatically falls back to a stale LOCAL database.
- Administrator can export an off-server recovery package.
- Emergency LOCAL Recovery is explicit and validates package paths + database SHA-256.
- Existing LOCAL target is preserved; business files apply only after database restore succeeds.
- Shared startup failure supports Retry / Recover from Package / Exit.

BUILD / TEST VERIFICATION
- Maven clean verify: BUILD SUCCESS.
- Server tests: 21 run, 0 failures, 0 errors, 2 opt-in integration tests skipped.
- Desktop tests: 69 run, 0 failures, 0 errors, 0 skipped.
- RELEASE_GATES_CURRENT_OK version=9.0.85.
- CLOUD_UAT_CONTRACT_OK version=9.0.85 env=UAT/PROD backup=WEEKLY/2 linux=yes.
- GITHUB_DEPLOYMENT_CONTRACT_OK uat=automatic prod=manual artifact=same rollback=binary.
- DISASTER_RECOVERY_CONTRACT_OK admin_only=yes fresh_snapshot=yes files=attachments,documents,templates local_switch=explicit.
- deploy-oracle-release.sh bash syntax: PASS.

UAT STATUS
- Live Oracle UAT remains on 9.0.83 because the first 9.0.84 automated deployment stopped before changing UAT when the protected env-file check failed.
- 9.0.85 contains the fix; do not tag/deploy until GitHub main is green and UAT environment secrets/variables remain configured.

STATUS
- VERIFIED FOR 9.0.85 SOURCE HANDOFF / GITHUB MAIN PUSH.
