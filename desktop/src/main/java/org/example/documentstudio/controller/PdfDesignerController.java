/*
 * DSE_PDF_DESIGNER_TOMBSTONE
 *
 * Compatibility tombstone for repositories that are updated by overlaying a release
 * instead of deleting files removed by newer versions.  The legacy PdfDesignerController
 * implementation was retired by PDF Studio 3.  This compilation unit deliberately
 * declares no Java type and has no runtime behavior.
 */
